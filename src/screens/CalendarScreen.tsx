import { PlaceholderScreen } from './PlaceholderScreen';

/**
 * Calendar tab main screen — "Calendario" in the UI.
 * Placeholder until the calendar feature is built.
 */
export function CalendarScreen() {
  return <PlaceholderScreen headerTitle="Calendario" contentText="Contenido" />;
}
