import { PlaceholderScreen } from './PlaceholderScreen';

/**
 * Schedule tab main screen — "Horario" in the UI.
 * Placeholder until the schedule feature is built.
 */
export function ScheduleScreen() {
  return <PlaceholderScreen headerTitle="Horario" contentText="Contenido" />;
}
