import { PlaceholderScreen } from './PlaceholderScreen';

/**
 * News tab main screen — "Noticias" in the UI.
 * Placeholder until the news feature is built.
 */
export function NewsScreen() {
  return <PlaceholderScreen headerTitle="Noticias" contentText="Contenido" />;
}
