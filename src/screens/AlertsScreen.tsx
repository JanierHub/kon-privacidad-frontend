import { PlaceholderScreen } from './PlaceholderScreen';

/**
 * Alerts tab main screen — "Alertas" in the UI.
 * Placeholder until the alerts feature is built.
 */
export function AlertsScreen() {
  return <PlaceholderScreen headerTitle="Alertas" contentText="Contenido" />;
}
